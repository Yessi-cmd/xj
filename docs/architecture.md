# 玄鉴技术架构

## 项目边界

玄鉴位于独立仓库 `D:\Code\xj`，GitHub 远程为 `Yessi-cmd/xj`。它不再依赖 AShare 的目录结构、Python 包或数据库；AShare 可继续独立承担量化研究，两者通过显式导出文件或未来 API 协作。

网站采用 React 19、vinext、TypeScript 与 `lunar-javascript`。公网版本构建为纯静态资源，由 Caddy 在 `xj.norliva.top` 提供服务；同一源代码也可发布到 Sites。

## 每日推演链路

1. 用户输入公历出生日期、时间、性别和出生城市。
2. 浏览器按城市经度校正简化真太阳时，并生成四柱与五行分布。
3. 系统以 `Asia/Shanghai` 为日期边界生成今日干支、流日五行和幸运提示。
4. 排序器将本命盘、流日、股票标签和反馈画像合成缘分分。
5. 六种职责分别选出守护、上签、潜龙、同曜、补运和相冲签；相冲签不属于正向推荐。
6. 相同本命、日期和换卦版本始终得到相同结果。

缘分分固定为：

```text
本命共振 45%
今日流日 25%
小众探索 15%
用户缘感 10%
基础资格 5%
```

用户缘感只在命理候选中微调，最终影响不超过总分 10%。正向命签执行七日重复冷却；守护签与相冲签不受该冷却影响。

## 股票标签构建

`public/data/mystic-stocks.json` 是运行时唯一股票数据源。每只股票可包含：

- 五行主象与副象
- 阴阳、星曜、神兽、卦宫与灵数
- 探索度
- 上市日期与上市日柱
- 交易所方位
- 真实行业及行业五行
- 标签版本

`npm run data:refresh` 在构建阶段通过 AkShare 的交易所批量列表补齐上市资料，再使用 `lunar-javascript` 生成上市日柱。缺少行业时不填入占位行业；缺少日期时排序器回退到代码灵数盘。生产页面不在运行时访问 AkShare。

## 本地状态与隐私

全部个人数据保存在版本化浏览器键 `xuanjian.state.v1` 中，包括：

- 本命档案
- 反馈向量和避开名单
- 缘分册
- 最近三十日星轨
- 当日换卦状态

反馈不会重新计算当天已经锁定的结果，只影响未来命签。本地状态损坏时安全回退为空档案。

跨设备迁移使用 `.xjprofile` 文件。用户密码通过 PBKDF2-SHA256 派生密钥，正文由 AES-GCM 加密；文件只保存版本、盐、IV 与密文，密码不落盘。分享卡不包含出生日期、时间、地点或登录邮箱。

## 发布

- `npm run build`：生成 Sites/Worker 兼容产物。
- `npm run build:vps`：额外生成 `dist-vps/` 静态目录，将 `/live` 渲染为根首页。
- Caddy 站点根目录通过版本化 release 和 `current` 符号链接切换，便于快速回滚。

站点始终明确展示“传统文化娱乐体验，不构成投资建议”，不提供实时行情、买卖指令或收益预测。
